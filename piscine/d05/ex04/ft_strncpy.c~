#include <stdio.h>

char	*ft_strncpy(char *dest, char *src, unsigned int n)
{
  int i;

  i = 0;
  while (i < n)
    {
      dest[i] = src[i];
      i++;
    }
  return (dest);
}

int	main(void)
{
  char src[] = "moi";
  char dest[1];

  printf("%s", ft_strncpy(dest, src, 3));
  return (0);
}
