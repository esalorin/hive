#include <stdio.h>

unsigned int ft_strlcpy(char *dest, char *src, unsigned int size)
{
  unsigned int i;

  i = 0;
  while ()
}

int	main(void)
{
  char dest[] = "moi";
  char src[] = "heippa";
  unsigned int size = 3;

  printf("%d", ft_strlcpy(dest, src, size));
  return (0);
}
